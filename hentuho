
#define INTEGER
//#define DOUBLE
//#define CHAR
#include"Function.h"
int main()
{
	srand(time(NULL));
#ifdef INTEGER
	int arr[SIZE];
#endif // INTEGER
#ifdef DOUBLE
	double arr[SIZE];
#endif // DOUBLE
#ifdef CHAR
	char arr[SIZE];
#endif // CHAR
	fillArray(arr);
	showArray(arr);
	getMax(arr);
	getMin(arr);
	arraySort(arr);
	cout << "max: " << getMax(arr);
	cout << "min: " << getMin(arr);
	cout << "arraySort: " << endl;
	showArray(arr);
}
